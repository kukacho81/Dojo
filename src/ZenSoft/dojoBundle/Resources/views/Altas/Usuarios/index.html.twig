{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Altas\Usuarios list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Password</th>
                <th>Email</th>
                <th>Idtiposdocumento</th>
                <th>Documento</th>
                <th>Imagen</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('abm_usuarios_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.password }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.idTiposdocumento }}</td>
                <td>{{ entity.documento }}</td>
                <td>{{ entity.imagen }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('abm_usuarios_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('abm_usuarios_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('abm_usuarios_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
