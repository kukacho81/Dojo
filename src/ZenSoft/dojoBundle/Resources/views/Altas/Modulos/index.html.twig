{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Altas\Modulos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Detalle</th>
                <th>Idasignaturas</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('abm_modulos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.detalle }}</td>
                <td>{{ entity.idAsignaturas }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('abm_modulos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('abm_modulos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('abm_modulos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
